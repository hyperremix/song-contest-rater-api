import { attribute, autoGeneratedHashKey, table } from '@aws/dynamodb-data-mapper-annotations';

@table(process.env.ACTS_TABLE)
export class ActDocument {
  @autoGeneratedHashKey()
  id: string;
  @attribute()
  artistName: string;
  @attribute()
  songName: string;
}
